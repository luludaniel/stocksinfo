# StocksInfo 개편 방향

작성일: 2026-08-02
전제: **중장기 보유 투자자 / 이메일 2~3분 소비 / 무료 데이터만**

---

## 1. 결론 먼저

지금 시스템은 **"뻔한 데이터를 예쁘게 포장하는 기계"** 입니다.
LLM 프롬프트를 아무리 고쳐도 뻔함은 안 사라집니다. **입력 데이터가 뻔하기 때문**입니다.

바꿔야 할 것은 이 한 줄입니다.

> **"매일 시장을 요약한다" → "어제와 달라진 것이 있을 때만, 내 종목에 대해 말한다"**

중장기 투자자에게 지수 일간 등락률은 **노이즈**입니다. 신호는 두 곳에만 있습니다.
**(a) 지금 어디쯤 와 있는가(위치)**, **(b) 무엇이 달라졌는가(변화)**.
현재 코드는 둘 다 수집하지 않습니다.

---

## 2. 왜 뻔한가 — 근본 원인 6가지

### ① 수집 데이터 자체가 공개 정보 수준
`us_market.py`가 모으는 것: S&P500·나스닥·다우 등락률, VIX, 섹터 ETF 등락률.
전부 네이버 금융 첫 화면에 있습니다. **정보 우위가 0인 입력**에서 정보 우위가 있는 출력은 나올 수 없습니다.

### ② 시야가 "이틀"밖에 없다 — 중장기 투자자에게 치명적
모든 collector가 `period="2d"`. 즉 **어제 대비 오늘**만 봅니다.

```python
# us_market.py, kr_market.py, watchlist_stocks.py 공통
data = yf.download(tickers, period="2d", ...)
```

중장기 투자자가 알아야 할 것은 "NVDA 오늘 +1.2%"가 아니라
"NVDA는 52주 밴드의 22% 지점, 200일선 대비 -12%, 고점 대비 -31%"입니다.
**맥락이 통째로 없습니다.**

### ③ 뉴스 파이프라인이 사실상 죽어 있다
`collectors/news.py`의 피드 4개 중 최소 3개가 무효로 보입니다.

| 피드 | 상태 |
|---|---|
| `feeds.reuters.com/reuters/businessNews` | 로이터가 공개 RSS를 폐기함 (2020년) → 404 |
| `feeds.reuters.com/reuters/companyNews` | 동일 |
| `finance.naver.com/news/news_list.naver?...` | **RSS가 아니라 HTML 페이지**. feedparser 파싱 불가 |
| `kr.investing.com/rss/news.rss` | 유일하게 살아있을 가능성 |

더 나쁜 건 이 부분입니다.

```python
except Exception:
    pass          # ← 조용히 삼킴
```

뉴스가 0건이어도 아무도 모릅니다. `_errors`에도 안 잡힙니다.
"③ 오늘의 핵심 이슈"가 매일 공허한 이유가 여기 있습니다.

### ④ LLM이 판단을 안 하고 서식만 채운다
현재 프롬프트는 사실상 *"이 JSON을 5개 섹션에 나눠 담아라"* 입니다.
비교도, 이상 탐지도, 판정도 코드가 안 해주니 LLM은 숫자를 문장으로 바꾸는 일만 합니다.
게다가 **max_tokens=900**으로 5개 섹션을 채우라니 각 항목이 두 줄짜리 껍데기가 됩니다.

### ⑤ 기억이 없다 — 매일 0에서 시작
저장소가 `watchlist.json` / `recipients.json` 뿐입니다.
어제 무슨 얘기를 했는지, 지난주 대비 뭐가 바뀌었는지 시스템이 모릅니다.
**그래서 매일 같은 소리를 합니다.** 이게 "뻔함"의 가장 큰 단일 원인입니다.

### ⑥ 근거 없는 매매 추천 → 신뢰 붕괴
프롬프트가 "진입가·손절가를 반드시 포함"하라고 요구하는데,
그 숫자를 계산할 데이터(지지/저항, 변동성, ATR)를 아무도 안 줍니다.
**LLM이 지어냅니다.** 한 번이라도 눈치채면 브리핑 전체의 신뢰가 무너집니다.
그리고 중장기 투자자에게 일일 손절가는 애초에 맞지 않는 정보입니다.

---

## 3. 부수적 결함 (고치기 쉬움)

| 항목 | 문제 | 조치 |
|---|---|---|
| `summarizer/claude_client.py` | `config.ANTHROPIC_API_KEY` 미정의 → import 즉시 크래시 | 삭제 또는 config 보강 |
| `summarizer/gemini_client.py` | `config.GOOGLE_API_KEY` 미정의 → 동일 | 동일 |
| `delivery/telegram.py` | `config.TELEGRAM_*` 미정의 → 동일 | 동일 |
| `config.py` | `CLAUDE_MODEL = "claude-sonnet-4-6"` — 실재하지 않는 모델 ID | 사용처 없으면 제거 |
| `openrouter_client.py` | 모델 ID 4개 중 상당수가 실재 의심<br>(`google/gemma-4-31b-it:free`, `moonshotai/kimi-k2.6:free`, `nvidia/nemotron-3-super-120b-a12b:free`) | OpenRouter `/api/v1/models`로 런타임 검증 후 폴백 |
| `economic_cal.py` | FRED는 **모든 엔드포인트에 `api_key` 필수**. 키 없이 호출 → 400.<br>`except: pass`로 조용히 실패 → 사실상 정적 리스트만 동작 | 무료 키 발급 후 정상화 |
| `.github/workflows` cron | `0 21 * * *` = 06:00 KST. 미국 장 마감 직후라<br>yfinance에 당일 종가가 아직 안 잡힐 수 있음 → **"전일"이 그저께일 위험** | 07:30 KST로 이동 + 데이터 날짜 검증 |
| `email_sender.py` | `html_body` 변수를 만들어놓고 안 씀 (dead code) | 정리 |
| 전 collector | `except: pass` 남용 | 실패를 `_errors`에 반드시 기록 |

---

## 4. 새 정보 구조 — 4개 축

무료 소스(주로 yfinance)만으로 전부 가능합니다. 핵심은 **무엇을 뽑느냐**입니다.

### 축 A. 위치 (Position) — "지금 어디쯤인가"
1일 등락률을 **버리고** 이걸로 대체합니다.

- 52주 밴드 내 백분위 (0%=저점, 100%=고점)
- 200일 이동평균 대비 이격도
- 전고점 대비 낙폭 (drawdown)
- 3 / 6 / 12개월 누적 수익률
- 벤치마크(S&P500, KOSPI) 대비 상대강도

> *"AAPL은 52주 밴드 88% 지점, 200일선 +14%, 최근 6개월 S&P500 대비 +9%p"*
> — 한 줄에 중장기 판단 재료가 다 들어갑니다. 일간 등락률 열 줄보다 낫습니다.

### 축 B. 변화 (Delta) — "어제와 무엇이 달라졌나" ★ 가장 중요
**SQLite에 매일 스냅샷을 저장하고, 브리핑은 어제 대비 변화만 다룹니다.**

탐지할 이벤트 예시:
- 200일선 상/하향 돌파 (몇 개월 만인지 함께)
- 52주 신고가 / 신저가
- 거래량이 20일 평균의 N배 초과
- 밸류에이션 밴드 이탈 (PER이 5년 평균 ±1σ 밖)
- 애널리스트 목표주가 컨센서스의 주간 변화율
- 실적발표 D-7 진입

> *"NVDA — 200일선 상향 돌파. 3개월 만에 처음."*
> 이런 문장은 어떤 뉴스에도 안 나옵니다. **히스토리가 있어야만 만들 수 있는 정보**입니다.

### 축 C. 펀더멘털 — 중장기 투자의 본체
yfinance로 전부 무료 수집 가능한데 **현재 하나도 안 씁니다.**

- PER / PBR / PSR + **본인의 과거 밴드 대비 위치**
- 분기 매출·영업이익 성장률 추이
- 실적발표 예정일 (`Ticker.earnings_dates`)
- 애널리스트 목표주가·투자의견 분포 (`Ticker.analyst_price_targets`)
- 배당수익률, 배당락일
- 부채비율, 잉여현금흐름

> *"PER 28배 — 5년 평균 22배 대비 27% 프리미엄"*
> 이건 뉴스에 안 나옵니다. 계산해야 나옵니다. **여기가 진짜 차별점입니다.**

### 축 D. 캘린더 — 내 종목에만 국한
전체 경제지표 나열은 버립니다. 대신:

> *"이번 주: NVDA 실적(수), AAPL 배당락(금), CPI 발표(목)"*

---

## 5. 데이터 소스 재정비 (전부 무료)

| 소스 | 용도 | 비고 |
|---|---|---|
| **yfinance** | 가격·기술지표·펀더멘털·실적일정·애널리스트 | 핵심. 지금 10%만 활용 중 |
| **FRED API** | 금리·CPI·실업률 시계열 | 무료 키 발급 필요 (현재 미발급) |
| **Google News RSS** | 종목명 쿼리 기반 뉴스 | `news.google.com/rss/search?q=...&hl=ko` |
| **yfinance `Ticker.news`** | 종목별 뉴스 | 이미 `watchlist_stocks.py`에 있음. 활용도만 올리면 됨 |
| **DART OpenAPI** | 한국 종목 공시 원문 | 무료. **한국 중장기 투자엔 뉴스보다 가치 높음** |
| **KRX 정보데이터시스템** | 외국인·기관 수급 | 무료 |
| ~~Reuters RSS~~ | — | 폐기됨. 제거 |
| ~~Naver 뉴스 URL~~ | — | RSS 아님. 제거 |

---

## 6. LLM 역할 재정의

**지금:** 데이터 → 서식 채우기 (숫자를 지어낼 여지가 있음)

**바꿀 구조:**

1. **규칙 엔진(Python)이 먼저 신호를 계산한다.**
   200일선 돌파, 밸류에이션 이탈, 거래량 이상, 실적 임박 — 숫자와 판정은 **코드가** 합니다.
   → LLM이 숫자를 창작할 여지가 구조적으로 사라집니다.

2. **LLM은 "신호가 잡힌 것"만 받아서 해석한다.**
   *왜 중요한가 / 무엇을 확인해야 하나*를 서술. 근거 숫자는 코드가 준 것만 인용하도록 강제.

3. **신호 0개면 LLM을 호출하지 않는다.**
   "오늘 특이사항 없음" 한 줄 발송. **비용 0, 신뢰 상승.**
   매일 억지로 5개 섹션을 채우는 것보다 훨씬 낫습니다.

4. `max_tokens` 900 → 2000. 단, 신호 개수에 비례해서만 길어지도록.

---

## 7. 이메일 레이아웃 (2~3분 소비 기준)

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━
📊 2026-08-03 (월)   신호 3건
━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔴 확인 필요 (2)
  NVDA    200일선 -8% 이탈 — 6개월 만에 처음
  005930  3분기 실적 D-3, 컨센서스 3주간 -12% 하향

🟡 관찰 (1)
  AAPL    PER 34배 — 5년 밴드 상단 (평균 27배)

⚪ 정상 범위 (3)
  TSLA · 000660 · 069500

📅 이번 주  NVDA 실적(수) · CPI(목) · AAPL 배당락(금)

─── 상세 ─────────────────────
[신호별 2~3문장 해설, 근거 숫자 포함]
```

**첫 5줄만 보고 3초 안에 "오늘 볼 게 있나"를 판단**할 수 있어야 합니다.
신호 0건인 날은 첫 블록 한 줄로 끝냅니다.

---

## 8. 단계별 로드맵

### Phase 1 — 정리 + 즉효 (반나절)
- 죽은 모듈 3개 정리 (`claude_client`, `gemini_client`, `telegram`)
- `news.py` 죽은 피드 교체, `except: pass` 제거 → 실패를 `_errors`에 기록
- **축 A(위치 지표) 추가** — 52주 백분위·200일선·drawdown·3/6/12M 수익률
- `max_tokens` 상향, cron 07:30 KST 이동
- 이메일 신호등 레이아웃 적용

→ **이것만으로 "뻔함"의 절반이 해소됩니다.**

### Phase 2 — 기억 만들기 (1~2일) ★ 핵심
- SQLite 히스토리 테이블 (`daily_snapshot`)
- 규칙 엔진 `signals.py` — 축 B 이벤트 탐지
- 신호 0건이면 LLM 호출 생략
- LLM 프롬프트 재작성 (요약가 → 해석가)

→ **"어제와 뭐가 다른가"가 생깁니다. 여기가 전환점입니다.**

### Phase 3 — 차별화 (2~3일)
- 축 C 펀더멘털 수집기 (PER 밴드, 실적일정, 애널리스트 추정치 변화)
- 축 D 종목 한정 캘린더
- DART 공시 연동 (한국 종목)

→ **뉴스에 없는 정보가 생깁니다.**

### Phase 4 — 선택
- 웹 대시보드에 차트·히스토리 뷰
- 신호 룰의 사후 적중률 추적 (자기 검증)

---

## 9. 우선순위 한 줄 요약

> **SQLite 히스토리 + 규칙 엔진(Phase 2)이 전체 개선의 70%입니다.**
> 프롬프트 손질은 마지막에 해도 됩니다.
